import { ContainerLayout } from "@/components/container";

export default function Dashboard() {
    return (
        <ContainerLayout>
            <h1>dashboard</h1>
        </ContainerLayout>
    );
}
